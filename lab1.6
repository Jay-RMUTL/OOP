import java.util.Scanner;

public class Main {
    public static void main(String[] rgs) {
        Scanner scanner = new Scanner(System.in);

        System.out.print("โปรดป้อนคะแนนสอบ (0-100): ");
        if (scanner.hasNextInt()) {
            int score = scanner.nextInt();
            String grade;

            if (score < 0 || score > 100) {
                grade = "คะแนนที่ป้อนไม่ถูกต้อง (ต้องอยู่ระหว่าง 0 ถึง 100)";
            } else {
                if (score >= 80) {
                    grade = "A";
                } else if (score >= 70) {
                    grade = "B";
                } else if (score >= 60) {
                    grade = "C";
                } else if (score >= 50) {
                    grade = "D";
                } else {
                    grade = "F";
                }
            }

            System.out.println("คะแนนที่ป้อน: " + score);
            if (grade.length() == 1) {
                System.out.println("คุณได้เกรด: " + grade);
            } else {
                System.out.println(grade);
            }

        } else {
            System.out.println("คุณป้อนข้อมูลไม่ถูกต้อง กรุณาป้อนตัวเลขจำนวนเต็มเท่านั้น");
        }

        scanner.close();
    }
}
